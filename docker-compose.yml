version: '3.7'

services:
  app:
    build: .
    image: "ghcr.io/weeb-poly/syncplay-server"
    environment:
      SYNCPLAY_PASSWORD: "${SYNCPLAY_PASSWORD}"
      SYNCPLAY_SALT: "${SYNCPLAY_SALT}"
      SYNCPLAY_PORT: "${SYNCPLAY_PORT}" 
    ports:
      - "${SYNCPLAY_PORT}:${SYNCPLAY_PORT}"
